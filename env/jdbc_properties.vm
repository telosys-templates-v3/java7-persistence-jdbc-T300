${SHARP} ---
${SHARP} Created on $today.date ( Time $today.time )
${SHARP} Generated by $generator.name ( version $generator.version )
${SHARP} ---
#if ( $databases.hasDatabase( $model.databaseId ) )
#set($database = $databases.getDatabase( $model.databaseId )) 
${SHARP} Database configuration  
jdbc.driverClassName=$database.driverClass
jdbc.url=$database.jdbcUrl
jdbc.username=$database.user 
jdbc.password=$database.password
#else
${SHARP} Example of database configuration : H2 is used as example
${SHARP}
${SHARP} JDBC driver class 
jdbc.driverClassName=org.h2.Driver
${SHARP} JDBC URL : H2 database 'in memory' with initialization script for tables creation
jdbc.url=jdbc:h2:mem:test;INIT=RUNSCRIPT FROM 'classpath:sql/create_h2_db.sql' 
${SHARP} User name
jdbc.username=sa 
${SHARP} User password
jdbc.password=sa
#end